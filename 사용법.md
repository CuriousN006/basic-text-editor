# 오프라인 리치 라이터 사용법

`index.html`은 설치 없이 Edge 또는 Chrome으로 실행하는 로컬 문서 편집기입니다.

## 실행

1. `index.html` 파일을 더블클릭합니다.
2. 연결 프로그램을 묻는 경우 Edge 또는 Chrome을 선택합니다.
3. 상단의 **파일 열기**로 `.md`, `.txt`, `.html` 파일을 불러옵니다.

인터넷 연결, 로그인, 서버, Node.js가 필요하지 않습니다.

## 네이버 카페에 복사

1. 편집을 마친 뒤 **전체 서식 복사**를 누릅니다.
2. 네이버 카페 글쓰기 창에서 `Ctrl+V`를 누릅니다.
3. 네이버가 일부 서식을 제거할 수 있으므로 게시 전 결과를 확인합니다.

**전체 서식 복사**와 편집 영역의 `Ctrl+C`는 `Shift+Enter`를 포함한 화면의 각 줄을 독립적인 HTML 블록으로 변환해 복사합니다. 네이버 카페가 `<br>`을 공백으로 바꾸더라도 줄이 합쳐지지 않으며, 빈 줄은 공백이 들어간 빈 블록으로 보존됩니다.

링크는 네이버 카페가 안정적으로 처리할 수 있도록 복사할 때 웹 주소(`http://`, `https://`)와 표시 글자만 남긴 단순한 하이퍼링크로 정리됩니다. 링크 안에서 실제로 서식이 지정된 글자 범위에만 굵게·기울임·밑줄·취소선 태그를 적용합니다. 로컬 파일 주소나 잘못된 주소는 링크를 제거하고 표시 글자만 복사합니다.

표는 네이버 편집기가 이 문서의 CSS를 가져오지 않아도 보이도록 테두리, 셀 여백과 제목 배경을 복사 HTML에 직접 넣습니다.

## 다른 곳의 글 붙여넣기

- `Ctrl+V`: **굵게, 기울임, 밑줄, 취소선, 하이퍼링크는 유지**합니다. 글꼴, 글자 크기, 글자 색, 배경색처럼 문서 전체 모양을 흔드는 외부 서식은 가져오지 않습니다.
- `Ctrl+Shift+V`: 서식을 모두 버리고 글자만 붙여넣습니다.

두 경우 모두 **줄 구조는 일반 텍스트를 그대로 따릅니다.** 줄바꿈 하나가 문단 하나가 되고, 원본에 빈 줄이 있으면 빈 줄로 들어옵니다. `Ctrl+V`와 `Ctrl+Shift+V`의 줄 구조는 항상 같고, `Ctrl+V`만 글자 서식을 함께 가져옵니다.

원본 HTML의 블록 구조를 그대로 끼워 넣지 않는 이유는 두 가지입니다. 문단이 쪼개지거나 붙여넣은 내용 뒤에 빈 줄이 하나 더 생기고, 채팅 화면처럼 문단 사이의 빈 줄이 HTML에는 없는 경우 빈 줄이 사라지기 때문입니다.

그래서 목록과 표는 구조가 아니라 줄로 들어옵니다. 목록 항목은 각각 문단이 되고, 표의 한 행은 칸 사이에 탭이 들어간 한 문단이 됩니다. 글자 서식은 그대로 유지됩니다. 표를 표로 넣으려면 **▦ 표**로 만들거나, 표가 들어 있는 마크다운·HTML 파일을 **파일 열기**로 불러오세요.

붙여넣기는 몇 줄을 넣었든 `Ctrl+Z` 한 번으로 되돌아갑니다.

글자를 선택한 상태에서 URL을 붙이면 선택한 글자에 링크가 걸립니다.

HTML을 붙여넣을 때 연속된 공백은 한 칸으로 줄어듭니다. 웹 페이지에서도 화면에 한 칸으로 보이기 때문입니다. 서식이 없는 일반 텍스트를 붙여넣을 때는 연속된 공백이 그대로 유지됩니다.

화면 폭 때문에 자동으로 꺾여 보이는 줄은 실제 줄바꿈이 아니므로 복사되지 않습니다. 원본에서 `Enter`나 `Shift+Enter`로 만든 줄바꿈만 보존할 수 있습니다.

## Word와 비슷하게 링크 넣기

1. 브라우저 주소창에서 URL을 `Ctrl+C`로 복사합니다.
2. 편집기에서 링크를 걸 글자를 선택합니다.
3. 그대로 `Ctrl+V`를 누릅니다.

또는 글자를 선택하고 `Ctrl+K`를 누른 뒤 URL을 붙여 넣습니다.

링크가 걸린 글자 위에 마우스를 올리면 연결 주소가 작은 팝업으로 표시됩니다. 링크와 팝업 사이에는 투명한 연결 영역이 있어 마우스를 팝업으로 옮겨도 닫히지 않습니다. 팝업의 주소를 클릭해 열거나 **주소 복사**를 누를 수 있습니다. 본문 속 링크를 직접 열려면 Word처럼 `Ctrl`을 누른 상태에서 클릭합니다.

## 서식 제거

글자를 선택하고 **서식 제거**를 누르면 굵게, 기울임, 링크뿐 아니라 제목 서식도 함께 제거되어 기본 본문으로 바뀝니다. 문서 내용을 전부 지우면 다음 입력부터 자동으로 기본 본문 서식이 적용됩니다.

커서 위치와 다음 입력 상태에 맞춰 상단의 **굵게**, **기울임**, **밑줄**, **취소선**, **왼쪽**, **가운데**, **오른쪽**, **글머리 목록**, **번호 목록** 버튼이 파란색으로 선택 표시됩니다. 정렬 버튼으로 현재 문단을 좌·중·우 정렬할 수 있습니다. 제목·본문·인용문·코드 블록 안으로 커서를 옮기면 문단 종류 선택 상자도 현재 위치에 맞게 바뀝니다.

글자를 드래그해 선택하면 선택 영역 가까이에 작은 서식 도구막대가 나타납니다. 여기서 **굵게**, **기울임**, **밑줄**, **취소선**을 바로 켜거나 끌 수 있습니다. 문서를 스크롤하거나 선택을 해제하면 도구막대가 닫힙니다.

## 표 넣기

상단의 **▦ 표**를 누르면 Word처럼 격자에서 원하는 행과 열을 마우스로 고를 수 있습니다. 격자보다 큰 표는 아래의 **행**, **열** 칸에 숫자를 직접 입력해 만듭니다. 기본값은 굵게나 가운데 정렬이 없는 일반 셀만 넣으며, 표 뒤에 빈 문단을 자동으로 만들지 않습니다. **첫 행을 제목 행으로 사용**을 직접 켠 경우에만 첫 줄이 굵은 글자와 배경색이 있는 제목 칸으로 만들어집니다.

표 안에서 `Tab`을 누르면 다음 칸으로, `Shift+Tab`을 누르면 이전 칸으로 이동합니다. 마지막 칸에서 `Tab`을 누르면 같은 열 개수의 새 행이 자동으로 추가되며, 이 동작만 따로 실행 취소하거나 다시 실행할 수 있습니다. HTML 저장과 전체 서식 복사에서는 표 모양과 테두리가 유지되며, DOCX 저장 시에는 Word의 실제 표 구조로 변환됩니다. MD 저장에서는 표를 HTML 표 문법으로 보존하므로 이 편집기로 다시 열어도 구조가 유지됩니다.

## 조사까지 자동으로 찾아 바꾸기

편집기에서 바꿀 글자를 드래그한 뒤 `Ctrl+H`를 누르면 선택한 글자가 **찾을 내용** 칸에 자동으로 들어가며, 입력칸 안에서도 전체 선택됩니다. 곧바로 다른 글자를 입력하면 기존 찾을 내용을 덮어쓸 수 있습니다.

`Ctrl+H`로 찾아 바꾸기 창을 열면 **조사 자동 교정**이 기본으로 켜집니다. 단어 바로 뒤의 조사를 새 단어의 받침에 맞춰 함께 바꿉니다.

**바꿀 서식**의 **굵게**, **기울임**, **밑줄**, **취소선** 버튼은 누를 때마다 **변경 없음 → 적용 → 제거** 순서로 바뀝니다. **서식 설정 초기화**를 누르면 네 버튼을 모두 **변경 없음**으로 되돌립니다. 글자를 선택하고 `Ctrl+H`를 누르면 찾을 내용과 바꿀 내용 입력칸에 현재 서식과 변경 결과가 미리 표시됩니다. 선택 범위 안에 서로 다른 서식이 섞여 있으면 입력칸 안내에 혼합 서식이라고 표시됩니다.

글자는 그대로 두고 서식만 일괄 변경하려면 찾을 내용을 입력하고 바꿀 내용은 비워 둡니다. 예를 들어 `중요`를 찾고 네 서식 버튼을 각각 **제거**로 지정한 다음 **모두 바꾸기**를 누르면 모든 `중요`가 일반 글자로 바뀝니다. 찾을 내용과 바꿀 내용을 모두 비우고 **모두 바꾸기**를 누르면 문서 전체의 서식을 버튼 상태대로 맞춥니다. 단어 중간에 굵게 같은 서식이 달라지는 경계가 있어도 화면에서 이어진 글자는 하나의 검색어로 찾습니다.

- `사과를`에서 `사과` → `책`: `책을`
- `책은`에서 `책` → `바나나`: `바나나는`
- `책으로`에서 `책` → `길`: `길로` (`ㄹ` 받침 예외 적용)

지원하는 조사는 `은/는`, `이/가`, `을/를`, `과/와`, `으로/로`, `이랑/랑`, `이나/나`, `이에요/예요`와 `으로서/로서`, `으로써/로써`, `으로부터/로부터`입니다. 입력한 글자만 그대로 바꾸려면 **조사 자동 교정**의 체크를 끕니다.

## 문단과 줄바꿈 찾아 바꾸기

Word처럼 찾기 또는 바꾸기 칸에서 다음 특수기호를 사용할 수 있습니다.

| 특수기호 | 의미 |
|---|---|
| `^p` | 문단 나눔 (`Enter`) |
| `^l` | 같은 문단 안의 줄바꿈 (`Shift+Enter`) |
| `^t` | 탭 |
| `^^` | `^` 문자 자체 |

예를 들어 찾기에 `^p^p`, 바꾸기에 `^p`를 입력하고 **모두 바꾸기**를 누르면 연속된 빈 문단을 줄일 수 있습니다. 찾기에 `^p`, 바꾸기에 `^l`을 입력하면 서로 다른 문단을 같은 문단 안의 줄바꿈으로 바꿉니다.

특수기호는 대문자와 소문자를 모두 인식합니다. 실제 `^p`라는 글자를 찾으려면 `^^p`를 입력합니다.

## 찾아 바꾸기 되돌리기

**모두 바꾸기**는 몇 개를 바꿨든 `Ctrl+Z` 한 번으로 전부 되돌아갑니다. **바꾸기**도 한 번에 한 단계씩 되돌아갑니다. 서식만 바꾸는 경우도 같습니다. `Ctrl+Y` 또는 `Ctrl+Shift+Z`로 다시 실행할 수 있습니다.

바꾼 뒤에 글자를 입력했다면 먼저 입력한 내용이 차례로 취소되고, 그 다음 `Ctrl+Z`에서 찾아 바꾸기가 취소됩니다. 한 번에 처리하는 일치 항목은 10,000개까지이며, 더 많으면 안내가 표시되므로 **모두 바꾸기**를 다시 누르면 됩니다.

## 주요 단축키

| 기능 | 단축키 |
|---|---|
| 굵게 | `Ctrl+B` |
| 기울임 | `Ctrl+I` |
| 밑줄 | `Ctrl+U` |
| 하이퍼링크 | `Ctrl+K` |
| 찾기 | `Ctrl+F` |
| 찾아 바꾸기 | `Ctrl+H` |
| 실행 취소 / 다시 실행 | `Ctrl+Z` / `Ctrl+Y` |
| 서식 유지 붙여넣기 | `Ctrl+V` |
| 서식 없이 붙여넣기 | `Ctrl+Shift+V` |
| HTML 저장 | `Ctrl+S` |

## 파일 형식

- **HTML 저장**: 서식을 가장 충실하게 보존합니다. `Ctrl+S`를 처음 누를 때만 저장 위치를 선택하고, 그다음부터는 같은 파일을 업데이트합니다. **파일 열기**로 기존 HTML 파일을 열었다면 그 파일을 바로 업데이트합니다.
- **MD 저장**: 다른 마크다운 프로그램과 공유할 때 사용합니다. MD 저장도 첫 저장 후에는 같은 파일을 업데이트합니다. 밑줄처럼 표준 마크다운에 없는 서식은 HTML 태그로 저장될 수 있습니다.
- **DOCX 저장**: 워드에서 직접 열고 편집할 수 있는 DOCX 파일을 만듭니다. 문단과 강제 줄바꿈, 제목, 굵게, 기울임, 밑줄, 취소선, 목록, 인용문, 코드 블록, 링크 및 표를 워드 문서 구조로 변환하며, 첫 저장 후에는 같은 파일을 업데이트합니다.
- Edge·Chrome의 파일 시스템 API를 지원하지 않는 브라우저에서는 기존 다운로드 방식으로 저장합니다.
- **마크다운 열기**: 제목, 굵게, 기울임, 취소선, 링크, 목록, 인용문, 코드 블록과 빈 줄을 지원합니다.

## 자동 저장과 파일 전환

글을 수정하면 입력을 잠시 멈춘 뒤 브라우저의 로컬 저장 공간에 복구용 초안을 기록합니다. 창을 닫거나 다른 탭으로 전환할 때는 남아 있는 변경 사항을 즉시 기록하며, 다음 실행 때 편집 영역이 비어 있으면 초안을 복원합니다. 복원이 일어나면 **저장하지 않은 내용을 복구했습니다** 안내가 표시되므로, 직접 지운 내용이 되살아난 것인지 바로 알 수 있습니다.

저장 위치를 직접 고르는 저장(파일 접근 권한을 지원하는 Edge·Chrome)이 정상적으로 끝나면 복구용 초안을 정리합니다.

브라우저 다운로드 방식으로 저장한 경우에는 초안을 지우지 않고 남겨 둡니다. 다운로드가 차단되거나 취소되어도 페이지에서는 성공과 구분할 수 없기 때문에, 초안까지 지우면 파일도 없고 복구 수단도 없는 상태가 되기 때문입니다. 이때 이탈 경고만 해제되고 초안은 다음 저장이나 **새 문서**까지 유지됩니다.

**새 문서**를 만들면 복구용 초안도 함께 비웁니다. 저장하지 않은 변경 내용이 있는 상태에서 다른 파일을 열거나 새 문서를 만들면 현재 문서를 닫을지 확인합니다.

## 주의사항

- 이 편집기는 브라우저의 리치 텍스트 기능을 사용합니다. 브라우저가 바뀌면 일부 동작이 조금 다를 수 있습니다.
- 네이버 카페 편집기가 허용하지 않는 색상, 간격 또는 특수 서식은 붙여넣을 때 제거될 수 있습니다.
- 중요한 문서는 작업 중간에 HTML로 저장해 두는 것이 안전합니다.
